{
  "name": "kiln-ts-monorepo",
  "private": true,
  "version": "0.1.0",
  "description": "Kiln: a Rails-style convention-over-configuration framework for TypeScript.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "license": "MIT"
}
